export enum LayoutAlgorithm {
  LAYERED = "layered",
  TREE = "tree",
  FORCE = "force",
  RADIAL = "radial",
}

export enum EdgeStyle {
  BEZIER = "bezier",
  STRAIGHT = "straight",
  STEP = "step",
}
